/**
 * §13.1 — prerendered. The filter is client state, not a URL parameter, so one
 * static page serves every combination; the skills themselves arrive from the
 * manifest at runtime like everywhere else (§7.1).
 */
export const prerender = true;
