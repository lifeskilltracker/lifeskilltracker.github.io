/** Scaffold constants aligned with svelte.config.js deploy settings. */
export const APP_TITLE = 'Life Skill Tracker';

/** adapter-static fallback page for GitHub Pages SPA routing (§4.4). */
export const STATIC_ADAPTER_FALLBACK = '404.html';

/** Environment variable read by svelte.config.js for kit.paths.base. */
export const BASE_PATH_ENV = 'BASE_PATH';

/** Root layout prerender flag so adapter-static emits index.html. */
export const PRERENDER_ROOT = true;
